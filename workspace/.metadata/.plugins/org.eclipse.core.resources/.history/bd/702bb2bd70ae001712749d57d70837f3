import java.util.Random;

public class RandomStringGenerator {
	String s;
	 Random rnd = new Random();
	 RandomStringGenerator(int n, String alfabet) {
		for (int i = 0; i < n; i++) {
			s.setCharAt(i, alfabet[rnd.nextInt(alfabet.length())]);
		}
	}
}
